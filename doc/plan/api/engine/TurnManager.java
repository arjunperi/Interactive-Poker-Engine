package api.engine;

import engine.Player;

public class TurnManager {
    /**
     * handles the order of turns in the game
     * knows who is up to play and what the order is, what round it is
     * each type of poker game might have a different order of turns, for example someone might go twice
     */

    /**
     * returns player who is up next to act
     */
    public Player getNextPlayer(){
        //will change
        return null;
    }

}

