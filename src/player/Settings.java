package player;

/**
 * This class is an example how the user might interact with the GUI, specifically at the start screen.
 */
public class Settings{
    /**
     * This method would take in a user input, get the starting amount, save it to a file for future use, and progress the game forward using this input.
     * We plan on allowing the users to access saved settings using login information, and by saving to files we will be able to provide
     * users with a dynamic user experience and interface that will better emulate a real poker game. 
     */
    public void setStartingAmount(){}
}
